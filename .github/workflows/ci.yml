name: CI

on:
  push:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . pytest ruff pyright
      - name: Ruff
        run: |
          ruff check checkota tests
          if [ -d scripts ]; then ruff check scripts; fi
      - name: Pyright
        run: |
          pyright checkota
          if [ -d scripts ]; then pyright scripts; fi
      - name: Tests
        run: python -m pytest -q
