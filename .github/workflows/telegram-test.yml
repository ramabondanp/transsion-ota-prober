name: TecnoNaman Telegram Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  telegram-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Test Telegram connection
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: "@tecnonamanupdate"
        run: |
          python - <<'PY'
          import json
          import os
          import urllib.request

          payload = json.dumps({
              "chat_id": os.environ["CHAT_ID"],
              "text": "✅ TecnoNaman OTA Tracker Telegram connection is working.",
          }).encode()
          req = urllib.request.Request(
              f"https://api.telegram.org/bot{os.environ['BOT_TOKEN']}/sendMessage",
              data=payload,
              headers={"Content-Type": "application/json"},
          )
          with urllib.request.urlopen(req, timeout=45) as response:
              body = json.loads(response.read().decode("utf-8"))
              if response.status != 200 or not body.get("ok"):
                  raise RuntimeError(f"Telegram API failed: {body}")
          print("Telegram connection test sent successfully")
          PY
